- name: Delete setup instance
  oci_compute_instance:
    instance_id: "{{ setup_windows_instance_id }}"
    state: absent
  register: result

- name: Delete setup nsg
  oci_network_security_group:
    network_security_group_id: "{{ setup_windows_instance_nsg_id }}"
    state: absent
  register: result
