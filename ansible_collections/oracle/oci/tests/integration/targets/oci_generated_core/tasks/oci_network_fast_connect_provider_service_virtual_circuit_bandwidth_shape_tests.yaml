---
- block:
  - name: List fast_connect_provider_service_virtual_circuit_bandwidth_shapes
    oci_network_fast_connect_provider_service_virtual_circuit_bandwidth_shape_facts:
      provider_service_id: "{{ dependency_provider_service_id | oracle.oci.override(test_fast_connect_provider_service_virtual_circuit_bandwidth_shape_list_provider_service_id, omit) }}"
    register: result
    when: skip_fast_connect_provider_service_virtual_circuit_bandwidth_shape_list_test is not defined

  - name: Assert resource was listed
    assert:
      that:
        - result.fast_connect_provider_service_virtual_circuit_bandwidth_shapes | length > 0
    when: skip_fast_connect_provider_service_virtual_circuit_bandwidth_shape_list_test is not defined


  - name: Assert required properties are populated
    assert:
      that:
        - item.name is defined
    with_items: "{{ result.fast_connect_provider_service_virtual_circuit_bandwidth_shapes }}"
    when: skip_fast_connect_provider_service_virtual_circuit_bandwidth_shape_list_test is not defined

  - name: Assert custom assertions
    assert:
      that:
        - "{{ item }}"
    with_items: "{{ fast_connect_provider_service_virtual_circuit_bandwidth_shape_list_custom_assertions }}"
    when: fast_connect_provider_service_virtual_circuit_bandwidth_shape_list_custom_assertions is defined

  always:
    - debug:
        msg: "Nothing to clean up since this resource does not have delete operation and also no dependencies."
